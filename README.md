# QA Portfolio

Добро пожаловать в мое портфолио QA-инженера!  
Здесь собраны примеры и шаблоны, которые демонстрируют мой подход к тестированию и документации.  

Репозиторий включает в себя шаблоны тест-кейсов, баг-репортов, чек-листов, а также запросы к API и базе данных.

---

## 📂 Структура репозитория

### **1. Postman/**
- `Collections/` — импортируемые коллекции Postman.  
- `Environments/` — файлы окружений для тестирования.  
- `Templates/` — шаблоны отдельных запросов (GET, POST, PUT, DELETE).  

**Пример использования:**  
1. Импортируйте коллекцию в Postman.  
2. Настройте окружение (`Environment`).  
3. Используйте шаблоны для быстрого создания новых запросов.

---

### **2. DB-Requests/**
- `Select-Templates/` — шаблоны SELECT-запросов.  
- `Insert-Templates/` — шаблоны INSERT-запросов.  
- `Update-Templates/` — шаблоны UPDATE-запросов.  
- `Delete-Templates/` — шаблоны DELETE-запросов.  

**Пример использования:**  
```sql
-- Получение всех пользователей
SELECT * FROM users WHERE is_active = 1;
